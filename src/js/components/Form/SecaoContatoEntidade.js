var m = require('mithril');

var SecaoContatoEntidade = {
	controller: function () {},
	view: function(ctrl) {
		return m('h3', 'Seção Contato Entidade');
	}
};

module.exports = SecaoContatoEntidade;

